#include<stdio.h>
#include<conio.h>
void main()
{
    int maths=53,sci=60,total;
    float per;
    total=maths+sci;
    per=total/2;
    printf("percentage is : %2f\n",per);
    printf("total is :%",total);
    getch();
}